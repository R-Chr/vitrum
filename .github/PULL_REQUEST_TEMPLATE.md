**What this changes**

<!-- One or two sentences. Link the issue it closes, if there is one. -->

**Why**

<!-- What was wrong, or what was missing. For a numerical fix, how you know the new answer
is the right one. -->

**Checklist**

- [ ] `pytest` passes
- [ ] `ruff check src tests` and `ruff format --check src tests` pass
- [ ] Added a test that fails without this change
- [ ] Updated the docs under `docs/` if behaviour changed
- [ ] Added a `CHANGELOG.md` entry
- [ ] **If this changes numbers previous versions returned, the changelog entry says so**
